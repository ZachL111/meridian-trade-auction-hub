# Review Journal

The cases below are the review handles I would use before changing the implementation.

The local checks classify each case as `ship`, `watch`, or `hold`. That gives the project a small review vocabulary that matches its trading systems focus without claiming live deployment or external usage.

## Cases

- `baseline`: `spread pressure`, score 231, lane `ship`
- `stress`: `fill risk`, score 184, lane `ship`
- `edge`: `portfolio drift`, score 175, lane `ship`
- `recovery`: `quote width`, score 152, lane `ship`
- `stale`: `spread pressure`, score 184, lane `ship`

## Note

This file is intentionally plain so the fixture remains the source of truth.
